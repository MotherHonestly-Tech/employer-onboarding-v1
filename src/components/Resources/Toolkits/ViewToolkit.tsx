import React, { Fragment } from "react";
import ViewHeader from "../SubComponents/ViewHeader";

type Props = {};

const ViewToolkit = (props: Props) => {
  return (
    <Fragment>
      <ViewHeader />
    </Fragment>
  );
};

export default ViewToolkit;
